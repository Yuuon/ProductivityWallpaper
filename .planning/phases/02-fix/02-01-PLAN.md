---
phase: 02-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CreatorView.xaml
  - ViewModels/CreatorViewModel.cs
  - Resources/LocalizationResources.xaml
autonomous: true
requirements:
  - FIX-01-001
  - FIX-01-002
  - FIX-01-003
  - FIX-01-004
  - FIX-01-005
must_haves:
  truths:
    - Left navigation shows single expanded submenu at a time
    - Navigation buttons have consistent styling
    - Scrollbar matches app theme
    - Feature pages display correctly when selected
    - Preview area shows only on Theme Preview page
  artifacts:
    - path: Views/CreatorView.xaml
      provides: Fixed Creator view with corrected navigation
    - path: ViewModels/CreatorViewModel.cs
      provides: Single expand logic and page navigation
  key_links:
    - from: CreatorViewModel.Is{Feature}Expanded
      to: CreatorView.xaml Visibility
---

<objective>
Fix Phase 01 Creator View navigation and layout issues as documented in Phase-01-Issue-Fix.md

**Purpose:** Resolve UI/UX issues in Creator view including navigation behavior, styling, and page display

**Output:** Fully functional Creator view with correct navigation behavior
</objective>

<execution_context>
@C:/Users/MA Huan/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@Phase-01-Issue-Fix.md
@Views/CreatorView.xaml (current implementation)
@ViewModels/CreatorViewModel.cs

**Issues to Fix:**
1. Theme preview left/right split ratio incorrect (should match ReferenceResources/创作主题 - 主题预览 - 空.svg)
2. Navigation should only expand one submenu at a time
3. Checkmark indicates active scheme, not selected page
4. New scheme button missing text and has wrong height
5. Scrollbar style doesn't match theme
6. Expandable nav buttons should have same style as simple buttons
7. Expanded submenu should not have background, only highlight current item
8. Arrow position too low when submenu expanded
9. Feature pages with submenus not displaying correctly
10. Theme preview left section showing on all pages instead of just Theme Preview
</context>

<interfaces>
Current CreatorViewModel structure for reference:
- bool IsDesktopBackgroundExpanded
- bool IsMouseClickExpanded
- bool IsShutdownExpanded
- bool IsBootRestartExpanded
- bool IsScreenWakeExpanded
- ObservableCollection<SchemeModel> DesktopBackgroundSchemes
- etc.
- ICommand SelectFeatureCommand
- ICommand CreateNewSchemeCommand
- ICommand SelectSchemeCommand
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Navigation Single-Expand Logic</name>
  <files>ViewModels/CreatorViewModel.cs</files>
  <action>
Implement single-expand logic for navigation:
1. When any expandable feature is expanded, collapse all others
2. Add method CollapseAllExcept(FeatureType feature)
3. Call this method when ToggleButton IsChecked changes
4. Ensure theme preview click collapses all expanded menus

Implementation:
```csharp
private void CollapseAllNavExcept(FeatureType? exceptFeature = null)
{
    if (exceptFeature != FeatureType.DesktopBackground)
        IsDesktopBackgroundExpanded = false;
    if (exceptFeature != FeatureType.MouseClick)
        IsMouseClickExpanded = false;
    if (exceptFeature != FeatureType.Shutdown)
        IsShutdownExpanded = false;
    if (exceptFeature != FeatureType.BootRestart)
        IsBootRestartExpanded = false;
    if (exceptFeature != FeatureType.ScreenWake)
        IsScreenWakeExpanded = false;
}
```

Call this in ToggleFeatureExpansion command and when selecting simple features.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Single-expand logic implemented, only one menu expands at a time</done>
</task>

<task type="auto">
  <name>Task 2: Fix Navigation Button Styling Consistency</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Fix navigation button styles for consistency:

1. **FeatureExpanderButtonStyle changes:**
   - Use same background as FeatureButtonStyle (BackgroundBlockBrush)
   - Use gradient highlight when selected (same as ActiveFeatureButtonStyle)
   - Remove distinct styling differences

2. **Simple button style (Theme Preview, Open App, etc.):**
   - Keep current but ensure hover/selected states match expander buttons

3. **Unify styles:**
   ```xml
   <!-- Base feature button style for ALL buttons -->
   <Style x:Key="BaseFeatureButtonStyle" TargetType="ButtonBase">
       <Setter Property="Background" Value="{StaticResource BackgroundBlockBrush}"/>
       <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
       <Setter Property="Height" Value="40"/>
       <Setter Property="Padding" Value="16,0"/>
       <Setter Property="Template">
           <Setter.Value>
               <ControlTemplate TargetType="ButtonBase">
                   <Border Background="{TemplateBinding Background}"
                           CornerRadius="10">
                       <ContentPresenter Margin="{TemplateBinding Padding}"
                                         VerticalAlignment="Center"/>
                   </Border>
               </ControlTemplate>
           </Setter.Value>
       </Setter>
   </Style>
   
   <!-- Active state with gradient -->
   <Style x:Key="ActiveBaseFeatureButtonStyle" BasedOn="{StaticResource BaseFeatureButtonStyle}">
       <Setter Property="Background" Value="{StaticResource PrimaryGradientBrush}"/>
   </Style>
   ```

4. **Arrow position fix:**
   - Move arrow to VerticalAlignment="Center" in ToggleButton
   - Current position may be defaulting to Stretch or incorrect value
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Navigation buttons have consistent styling, arrow positioned correctly</done>
</task>

<task type="auto">
  <name>Task 3: Fix Submenu Styling</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Fix submenu appearance:

1. **Remove submenu background:**
   ```xml
   <Border Visibility="{Binding IsDesktopBackgroundExpanded, Converter={StaticResource BooleanToVisibilityConverter}}"
           Background="Transparent"  <!-- Changed from BackgroundBlockBrush -->
           BorderThickness="0">
   ```

2. **Highlight current scheme item:**
   - In SchemeItemTemplate, add highlighting for selected scheme
   - Use Accent1Brush for border/background when selected
   - Current style already has IsActive trigger, verify it works

3. **Fix new scheme button:**
   - Add proper text: "+ New Scheme"
   - Increase height to match other buttons (min 32px)
   - Ensure padding is correct
   ```xml
   <Button Content="+ New Scheme"
           Height="32"
           Padding="12,6"
           ... />
   ```

4. **Active scheme indication:**
   - Checkmark should show when scheme.IsActive (already implemented)
   - This is different from selected scheme for editing
   - Ensure SelectedScheme property exists and is used for editing highlight
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Submenu has no background, current item highlighted, new scheme button styled correctly</done>
</task>

<task type="auto">
  <name>Task 4: Fix Scrollbar Style</name>
  <files>Resources/Theme.xaml</files>
  <action>
Add custom scrollbar style to match app theme:

1. **Add ScrollViewer style to Theme.xaml:**
   ```xml
   <Style TargetType="ScrollViewer">
       <Setter Property="Template">
           <Setter.Value>
               <ControlTemplate TargetType="ScrollViewer">
                   <Grid>
                       <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"/>
                       <ScrollBar x:Name="PART_VerticalScrollBar"
                                  Orientation="Vertical"
                                  Value="{TemplateBinding VerticalOffset}"
                                  Maximum="{TemplateBinding ScrollableHeight}"
                                  ViewportSize="{TemplateBinding ViewportHeight}"
                                  Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                  Width="8"
                                  Margin="0,4,4,4"
                                  HorizontalAlignment="Right"/>
                   </Grid>
               </ControlTemplate>
           </Setter.Value>
       </Setter>
   </Style>
   ```

2. **Add ScrollBar style:**
   ```xml
   <Style TargetType="ScrollBar">
       <Setter Property="Background" Value="Transparent"/>
       <Setter Property="Width" Value="8"/>
       <Setter Property="Template">
           <Setter.Value>
               <ControlTemplate TargetType="ScrollBar">
                   <Grid Background="{TemplateBinding Background}">
                       <Track x:Name="PART_Track"
                              IsDirectionReversed="True"
                              Width="{TemplateBinding Width}">
                           <Track.DecreaseRepeatButton>
                               <RepeatButton Command="ScrollBar.PageUpCommand"
                                             Opacity="0"/>
                           </Track.DecreaseRepeatButton>
                           <Track.Thumb>
                               <Thumb>
                                   <Thumb.Template>
                                       <ControlTemplate TargetType="Thumb">
                                           <Border Background="{StaticResource BorderLineBrush}"
                                                   CornerRadius="4"
                                                   Width="4"
                                                   HorizontalAlignment="Center"/>
                                       </ControlTemplate>
                                   </Thumb.Template>
                               </Thumb>
                           </Track.Thumb>
                           <Track.IncreaseRepeatButton>
                               <RepeatButton Command="ScrollBar.PageDownCommand"
                                             Opacity="0"/>
                           </Track.IncreaseRepeatButton>
                       </Track>
                   </Grid>
               </ControlTemplate>
           </Setter.Value>
       </Setter>
       <Style.Triggers>
           <Trigger Property="IsMouseOver" Value="True">
               <Setter Property="Background" Value="{StaticResource BackgroundBlockBrush}"/>
           </Trigger>
       </Style.Triggers>
   </Style>
   ```
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Custom scrollbar style applied matching app theme</done>
</task>

<task type="auto">
  <name>Task 5: Fix Feature Page Display</name>
  <files>Views/CreatorView.xaml, ViewModels/CreatorViewModel.cs</files>
  <action>
Fix feature content display:

1. **Fix LoadFeatureContent method:**
   - Ensure it properly loads the view for expandable features
   - Check factory methods are correctly resolving ViewModels
   - Add debug logging if needed to verify execution

2. **Content switching logic:**
   ```csharp
   private void LoadFeatureContent(string featureName)
   {
       // Reset preview and content
       HasPreviewContent = false;
       PreviewContent = null;
       ConfigurationContent = null;
       
       switch (featureName)
       {
           case "ThemePreview":
               // Only Theme Preview shows the left/right split
               PreviewContent = GetThemePreview();
               ConfigurationContent = GetThemeConfiguration();
               HasPreviewContent = true;
               break;
               
           case "DesktopBackground":
               // These features show configuration only, no left preview
               var dbVM = _serviceProvider.GetRequiredService<DesktopBackgroundViewModel>();
               dbVM.LoadScheme(GetCurrentSchemeId(FeatureType.DesktopBackground));
               ConfigurationContent = dbVM;
               break;
               
           // ... other cases
       }
   }
   ```

3. **Theme Preview only shows preview area:**
   - The left/right split should only appear for Theme Preview
   - Other features should show full-width configuration
   - Modify Grid layout to conditionally show/hide left column
   
   In CreatorView.xaml:
   ```xml
   <Grid Grid.Row="1">
       <Grid.ColumnDefinitions>
           <ColumnDefinition Width="{Binding IsThemePreviewSelected, Converter={StaticResource BooleanToGridLengthConverter}, ConverterParameter='*'}"/>
           <ColumnDefinition Width="{Binding IsThemePreviewSelected, Converter={StaticResource BooleanToGridLengthConverter}, ConverterParameter='*'}"/>
       </Grid.ColumnDefinitions>
       
       <!-- Left: Preview (Only for Theme Preview) -->
       <Border Grid.Column="0"
               Visibility="{Binding IsThemePreviewSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
               ...>
           ...
       </Border>
       
       <!-- Right: Configuration -->
       <Border Grid.Column="{Binding IsThemePreviewSelected, Converter={StaticResource BooleanToColumnIndexConverter}}"
               ...>
           <ContentControl Content="{Binding ConfigurationContent}"/>
       </Border>
   </Grid>
   ```

4. **Add BooleanToColumnIndexConverter:**
   Returns 0 when false, 1 when true (for Grid.Column)
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Feature pages display correctly, Theme Preview shows split layout, others show full config</done>
</task>

<task type="auto">
  <name>Task 6: Fix Split Ratio for Theme Preview</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Adjust Theme Preview split ratio to match SVG reference:

From ReferenceResources/创作主题 - 主题预览 - 空.svg:
- Left (preview) should be approximately 60% of width
- Right (config) should be approximately 40% of width

Change:
```xml
<Grid.ColumnDefinitions>
    <!-- Left: Preview - 60% -->
    <ColumnDefinition Width="6*"/>
    <!-- Right: Configuration - 40% -->
    <ColumnDefinition Width="4*"/>
</Grid.ColumnDefinitions>
```

Current is 50/50 (* *), needs to be 60/40 (6* 4*)
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Theme Preview split ratio is 60/40 (preview/config)</done>
</task>

</tasks>

<verification>
- [ ] Clicking expandable nav button collapses others
- [ ] Navigation button styles are consistent
- [ ] Scrollbar has theme-matching style (thin, dark, rounded)
- [ ] Submenu has transparent background
- [ ] Current scheme item is highlighted
- [ ] New scheme button shows "+ New Scheme" with proper height
- [ ] Arrow is vertically centered
- [ ] Desktop Background page shows correctly
- [ ] Mouse Click page shows correctly
- [ ] Other feature pages show correctly
- [ ] Theme Preview shows 60/40 split
- [ ] Other features show full-width configuration only
</verification>

<success_criteria>
All issues from Phase-01-Issue-Fix.md resolved:
- ✅ Theme preview split ratio 60/40
- ✅ Single expand navigation
- ✅ Consistent button styling
- ✅ Theme scrollbar
- ✅ Correct page display
- ✅ Theme preview shows only on its page
</success_criteria>

<output>
After completion, create `.planning/phases/02-fix/02-01-SUMMARY.md` documenting all fixes applied.
</output>
