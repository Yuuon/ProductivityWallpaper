---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Views/DesktopBackgroundView.xaml
  - ViewModels/DesktopBackgroundViewModel.cs
  - Models/MediaItemModel.cs
  - Views/PreviewWindow.xaml
autonomous: true
requirements:
  - REQ-UI-003
  - REQ-UI-004
must_haves:
  truths:
    - Desktop Background page shows upload UI when empty
    - Shows edit UI with file lists when has content
    - Supports image/video files with thumbnails
    - Supports audio files with separate list
    - Preview window displays media content
  artifacts:
    - path: Views/DesktopBackgroundView.xaml
      provides: Desktop background configuration UI
      min_lines: 200
    - path: Models/MediaItemModel.cs
      provides: Media file data model
      exports: ["MediaItemModel", "MediaType"]
  key_links:
    - from: DesktopBackgroundViewModel.MediaItems
      to: DesktopBackgroundView.xaml ListView
      via: ItemsSource binding
---

<objective>
Create Desktop Background configuration pages: empty state (upload) and content state (edit with file management).

**Purpose:** Allow users to configure desktop wallpaper content with image/video/audio files, playback settings, and display options.

**Output:** DesktopBackgroundView with upload and edit modes, media file lists with thumbnails, and preview window.
</objective>

<execution_context>
@C:/Users/MA Huan/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
</execution_context>

<context>
@SVG2XAML_GUIDE_2.md (lines 14-25)
@Views/CreatorView.xaml (current structure)
@ViewModels/CreatorViewModel.cs

**Reference SVGs:**
- ReferenceResources/创作主题 - 上传文件.svg (upload page)
- ReferenceResources/创作主题 - 编辑文件.svg (edit page)
- ReferenceResources/创作主题 - 编辑文件 - 查看图片.svg (image preview)
- ReferenceResources/创作主题 - 编辑文件 - 播放视频.svg (video preview)
- ReferenceResources/创作主题 - 编辑文件 - 播放语音.svg (audio preview)

**Icon Resources:**
- Resources/Img/Icon-upload.svg
- Resources/Img/Icon-preview.svg
- Resources/Img/PlayButton.svg
- Resources/Img/Icon-music.svg
- Resources/Img/Icon-mute.svg
- Resources/Img/Icon-unmute.svg
</context>

<interfaces>
From Models/MediaItemModel.cs (to be created):
```csharp
public enum MediaType
{
    Image,
    Video,
    Audio
}

public enum DisplayMode
{
    Fill,       // 填充（推荐）
    Center,     // 居中
    Tile        // 平铺（只限图片）
}

public enum PlaybackMode
{
    Sequential, // 顺序循环
    Random      // 随机
}

public class MediaItemModel
{
    public string Id { get; set; }
    public string FilePath { get; set; }
    public string FileName { get; set; }
    public MediaType Type { get; set; }
    public string Format { get; set; }
    public long FileSize { get; set; }
    public TimeSpan? Duration { get; set; }
    public bool IsMuted { get; set; }
    public DisplayMode DisplayMode { get; set; }
    public string ThumbnailPath { get; set; }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MediaItemModel</name>
  <files>Models/MediaItemModel.cs</files>
  <action>
Create MediaItemModel.cs with:
1. MediaType enum (Image, Video, Audio)
2. DisplayMode enum (Fill, Center, Tile)
3. PlaybackMode enum (Sequential, Random)
4. MediaItemModel class with properties:
   - Id (Guid)
   - FilePath (string)
   - FileName (string) 
   - Type (MediaType)
   - Format (string extension)
   - FileSize (long, bytes)
   - Duration (TimeSpan?)
   - IsMuted (bool)
   - DisplayMode (DisplayMode)
   - ThumbnailPath (string)
   - OrderIndex (int)

Extend from ObservableObject for binding support.
Add validation: DisplayMode.Tile only valid for MediaType.Image.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>MediaItemModel compiles with all enums and properties</done>
</task>

<task type="auto">
  <name>Task 2: Create DesktopBackgroundViewModel</name>
  <files>ViewModels/DesktopBackgroundViewModel.cs</files>
  <action>
Create DesktopBackgroundViewModel.cs with:

**Properties:**
- SchemeName (string) - current scheme name
- IsEditingName (bool) - toggle between Label and TextBox
- ObservableCollection<MediaItemModel> ImageVideoItems
- ObservableCollection<MediaItemModel> AudioItems
- PlaybackMode SelectedPlaybackMode (for image/video)
- PlaybackMode SelectedAudioPlaybackMode
- bool HasContent (ImageVideoItems.Count > 0)

**Commands:**
- [RelayCommand] ImportMedia() - open file dialog
- [RelayCommand] ImportAudio() - open audio file dialog  
- [RelayCommand] RemoveMedia(MediaItemModel item)
- [RelayCommand] ToggleMute(MediaItemModel item)
- [RelayCommand] PreviewMedia(MediaItemModel item)
- [RelayCommand] ToggleEditName()
- [RelayCommand] FinishEditName()

**File Dialog Filter:**
- Images: JPG, PNG, WebP, BMP, HEIC, SVG, GIF, APNG
- Videos: MP4, MOV, WebM
- Audio: MP3, WAV, OGG, etc.
- Max file size: 500MB (validate before adding)

Use OpenFileDialog with multiselect support.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>ViewModel compiles with all commands and properties</done>
</task>

<task type="auto">
  <name>Task 3: Create Empty State UI (Upload Page)</name>
  <files>Views/DesktopBackgroundView.xaml</files>
  <action>
Create DesktopBackgroundView.xaml with empty state (4 sections):

**Section 1: Editable Title Bar**
- Label showing scheme name + edit icon (Path with pencil)
- Click to toggle TextBox for editing
- LostFocus or Enter key to save
- "Activate This Scheme" button on right (or "Current Active Scheme" if active)

**Section 2: Import Header**
- Title: "Import Media Library"
- Style: SubHeaderTextStyle

**Section 3: Supported Formats Info**
- Text: "Supported formats: JPG, PNG, WebP, BMP, HEIC, SVG, GIF, APNG, MP4, MOV, WebM"
- Text: "Max file size: 500MB"
- Text: "Max resolution: 8K (7600x4320)"
- Style: BodyTextStyle with Secondary color

**Section 4: Upload Area**
- Large upload cloud icon (Icon-upload.svg)
- Clickable Border with dashed border style
- Text: "Click to Import"
- Subtext: "or drag and drop files here"
- Command binding to ImportMedia

**Layout:** Vertical StackPanel with consistent spacing (16px between sections)
**Visibility:** Show when HasContent=false
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Empty state UI displays with all 4 sections</done>
</task>

<task type="auto">
  <name>Task 4: Create Edit State UI (File Management)</name>
  <files>Views/DesktopBackgroundView.xaml</files>
  <action>
Add edit state to DesktopBackgroundView.xaml (visible when HasContent=true):

**Section 1: Editable Title Bar** (same as empty state)

**Section 2: Action Bar**
- Title: "Edit Files" on left
- Three round buttons on right:
  - "Continue Import" button (plus icon)
  - "Preview" button (eye icon)
  - "Delete" button (trash icon)

**Section 3: Image/Video List**
- Header row: "Images/Videos" + PlaybackMode ComboBox (Sequential, Random)
- ListView with custom ItemTemplate:
  - #Number (auto-increment)
  - Thumbnail (Image with fallback)
    - Images: show thumbnail
    - Videos: thumbnail + PlayButton.svg overlay
  - File format
  - File size (formatted: KB, MB)
  - Duration (for videos) or empty
  - Mute toggle button (Icon-mute.svg / Icon-unmute.svg)
  - DisplayMode ComboBox (Fill, Center, Tile)
    - Disable Tile for videos
  - Remove button (X icon)
- Right-align mute, combo, remove buttons

**Section 4: Audio List**
- Header row: "Audio" + PlaybackMode ComboBox
- Same layout as Image/Video but:
  - Thumbnail placeholder with Icon-music.svg + PlayButton.svg
  - No DisplayMode dropdown (audio has no display)
- Right-align mute and remove buttons

**Styling:**
- Use BackgroundBlockBrush for list containers
- BorderLineBrush for separators
- Consistent 16px padding
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Edit state UI displays with file lists and controls</done>
</task>

<task type="auto">
  <name>Task 5: Create Preview Window</name>
  <files>Views/PreviewWindow.xaml, Views/PreviewWindow.xaml.cs</files>
  <action>
Create PreviewWindow.xaml for media preview:

**Window Chrome:**
- Custom title bar with min/max/close buttons (like MainWindow)
- Title: "Preview - {FileName}"
- Size: 800x600, resizable

**Content:**
- Grid with single content area
- Image preview: Image control with Stretch=Uniform
- Video preview: MediaElement or use existing VideoPlayerWindow pattern
- Audio preview: 
  - Center: Icon-music.svg (large)
  - Below: Simple play/pause controls
  - Progress bar

**DataContext:**
- MediaItemModel item to preview
- IsPlaying bool
- CurrentPosition TimeSpan

**Code-behind:**
- Handle media loading
- Auto-play on open
- Cleanup on close

Reference VideoPlayerWindow.xaml for video playback implementation.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Preview window compiles and can display images</done>
</task>

</tasks>

<verification>
- [ ] Empty state shows upload UI with supported formats info
- [ ] Edit state shows file lists with thumbnails
- [ ] Image/Video list has correct columns and controls
- [ ] Audio list displays with music icon
- [ ] Preview window opens and displays media
- [ ] File size displays formatted (KB, MB)
- [ ] Duration shows for videos/audio
- [ ] Mute toggle switches icon
- [ ] DisplayMode combo disables Tile for videos
</verification>

<success_criteria>
Desktop Background page meets SVG2XAML_GUIDE_2.md lines 14-25:
- ✅ Empty state with upload UI and format info
- ✅ Edit state with file lists and controls
- ✅ Image/Video list with thumbnails, format, size, duration
- ✅ Audio list with music icon
- ✅ Playback mode dropdowns
- ✅ Display mode dropdowns (with Tile disabled for videos)
- ✅ Mute/unmute toggle buttons
- ✅ Preview window for media
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- Media item model created
- Desktop background upload and edit pages
- Preview window implementation
</output>
