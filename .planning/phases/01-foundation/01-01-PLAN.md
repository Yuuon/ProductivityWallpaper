---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CreatorView.xaml
  - ViewModels/CreatorViewModel.cs
  - Models/SchemeModel.cs
  - Resources/LocalizationResources.xaml
autonomous: true
requirements:
  - REQ-UI-001
  - REQ-UI-002
must_haves:
  truths:
    - Left navigation has expandable submenus for 5 features
    - Each feature supports multiple schemes
    - Only one scheme can be active per feature at a time
    - Scrollbar appears when content overflows
  artifacts:
    - path: Views/CreatorView.xaml
      provides: Left navigation with expandable menus
      min_lines: 150
    - path: Models/SchemeModel.cs
      provides: Scheme data model
      exports: ["SchemeModel", "FeatureType"]
  key_links:
    - from: CreatorViewModel.Schemes
      to: CreatorView.xaml ItemsSource
      via: DataBinding
---

<objective>
Update the left navigation in CreatorView to support expandable submenus for multi-scheme management.

**Purpose:** Enable users to create and manage multiple schemes per feature (Desktop Background, Mouse Click, Shutdown, Boot Restart, Screen Wake).

**Output:** Updated navigation UI with expandable/collapsible submenus, active scheme highlighting, and "New Scheme" button.
</objective>

<execution_context>
@C:/Users/MA Huan/.config/opencode/get-shit-done/workflows/execute-plan.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
</execution_context>

<context>
@SVG2XAML_GUIDE_2.md (lines 8-13)
@Views/CreatorView.xaml
@ViewModels/CreatorViewModel.cs

**Current State:**
- Left navigation has simple buttons for each feature
- No scheme management capability
- No expandable submenus

**Target State:**
- 5 features have expandable submenus (Desktop Background, Mouse Click, Shutdown, Boot Restart, Screen Wake)
- Each submenu shows existing schemes + "New Scheme" button
- Active scheme highlighted with checkmark
- Expand/collapse arrow indicators
- Vertical scrollbar when content overflows
</context>

<interfaces>
From Models/SchemeModel.cs (to be created):
```csharp
public enum FeatureType
{
    DesktopBackground,
    MouseClick,
    Shutdown,
    BootRestart,
    ScreenWake,
    OpenApp,
    DesktopClock,
    Pomodoro,
    Anniversary
}

public class SchemeModel
{
    public string Id { get; set; }
    public string Name { get; set; }
    public FeatureType FeatureType { get; set; }
    public bool IsActive { get; set; }
    public DateTime CreatedAt { get; set; }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SchemeModel</name>
  <files>Models/SchemeModel.cs</files>
  <action>
Create SchemeModel.cs with:
1. FeatureType enum for all 9 features
2. SchemeModel class with Id, Name, FeatureType, IsActive, CreatedAt properties
3. Use ObservableObject base from CommunityToolkit.Mvvm for data binding support
4. Add XML documentation for each property
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>SchemeModel.cs compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Update CreatorViewModel for Scheme Management</name>
  <files>ViewModels/CreatorViewModel.cs</files>
  <action>
Extend CreatorViewModel with:
1. ObservableCollection<SchemeModel> Schemes property for each of 5 multi-scheme features
2. Dictionary<FeatureType, ObservableCollection<SchemeModel>> SchemesByFeature
3. ObservableProperty bool Is{Feature}Expanded for each of 5 features
4. ObservableProperty SchemeModel SelectedScheme for each feature
5. RelayCommand ToggleFeatureExpansionCommand to expand/collapse menus
6. RelayCommand CreateNewSchemeCommand with auto-naming ("Desktop Background 1", etc.)
7. RelayCommand SelectSchemeCommand to activate a scheme
8. Logic: auto-create default scheme when feature has none

Use patterns from CONVENTIONS.md:
- Private fields with _ prefix
- [ObservableProperty] attribute for auto-generated properties
- [RelayCommand] attribute for commands
- Async suffix for async methods
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>ViewModel compiles with new scheme management properties</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Left Navigation XAML</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Redesign left navigation panel (lines 66-201):

1. **Feature Button Template:**
   - Main button shows feature name + count badge
   - Arrow icon (up/down) on right side indicating expand state
   - Click toggles expansion (not navigation)

2. **Expanded Submenu:**
   - List of scheme names with active indicator (checkmark)
   - Active scheme highlighted with different background
   - "New Scheme" button at bottom with distinct style
   - Smooth expand/collapse animation

3. **Single-Feature Buttons:**
   - OpenApp, DesktopClock, Pomodoro, Anniversary remain simple buttons
   - No submenu needed

4. **ScrollViewer:**
   - Wrap entire navigation in ScrollViewer
   - VerticalScrollBarVisibility="Auto"
   - Keep back button fixed at top (outside scroll)

5. **Styles:**
   - Use existing BackgroundBlockBrush for sidebar
   - Feature button style from BooleanToFeatureButtonConverter
   - Submenu item style with hover effects
   - Checkmark icon for active scheme

Reference SVG files for visual design:
- ReferenceResources/创作主题 - 空.svg (overall layout)
- ReferenceResources/创作主题 - 主题预览 - 有配置.svg (navigation appearance)
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>XAML compiles with new navigation structure</done>
</task>

<task type="auto">
  <name>Task 4: Add Localization Strings</name>
  <files>Resources/LocalizationResources.xaml</files>
  <action>
Add new localization keys to LocalizationResources.xaml:

English keys:
- "Creator_NewScheme" = "New Scheme"
- "Creator_ActiveScheme" = "Active"
- "Creator_DefaultSchemeName" = "{0} {1}" (e.g., "Desktop Background 1")
- "Creator_NoSchemes" = "No schemes yet"

Chinese keys (add to zh-CN.json if exists, or create):
- "Creator_NewScheme" = "新建方案"
- "Creator_ActiveScheme" = "当前激活"
- "Creator_DefaultSchemeName" = "{0} {1}"
- "Creator_NoSchemes" = "暂无方案"

Add to both Resources/LocalizationResources.xaml and Resources/Languages/zh-CN.json
  </action>
  <verify>
    <automated>grep -i "Creator_NewScheme" Resources/LocalizationResources.xaml && echo "Keys added"</automated>
  </verify>
  <done>Localization keys added for all new UI text</done>
</task>

</tasks>

<verification>
- [ ] Left sidebar shows 5 features with expand arrows
- [ ] Clicking feature button expands/collapses submenu
- [ ] Submenu shows scheme list with active checkmark
- [ ] "New Scheme" button visible at bottom of expanded menu
- [ ] Scrollbar appears when navigation content overflows
- [ ] Auto-creates default scheme when feature first expanded
- [ ] Selecting scheme updates active state in UI
</verification>

<success_criteria>
Navigation meets all requirements from SVG2XAML_GUIDE_2.md lines 8-13:
- ✅ Multi-scheme support for 5 features
- ✅ Expandable/collapsible submenus
- ✅ Active scheme highlighting with checkmark
- ✅ "New Scheme" button at bottom
- ✅ Vertical scrollbar when needed
- ✅ Arrow indicators for expand state
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Scheme data model created
- Navigation UI redesigned with submenus
- Multi-scheme management functional
</output>
