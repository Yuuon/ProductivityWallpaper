---
phase: 03-fix-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Views/CreatorView.xaml
  - ViewModels/CreatorViewModel.cs
  - Resources/Theme.xaml
  - Resources/LocalizationResources.xaml
  - App.xaml
autonomous: true
requirements:
  - FIX-V2-001
  - FIX-V2-002
  - FIX-V2-003
  - FIX-V2-004
  - FIX-V2-005
must_haves:
  truths:
    - Theme Preview split is 70/30
    - Only one nav button highlighted at a time
    - Only one submenu expanded at a time
    - Scheme items show highlight when selected
    - New scheme button shows "+ 新建方案"
    - Feature pages display correctly
    - Scrollbar thumb is brighter
  artifacts:
    - path: Views/CreatorView.xaml
      provides: Fixed Creator view
    - path: ViewModels/CreatorViewModel.cs
      provides: Fixed navigation logic
---

<objective>
Fix remaining Phase 01 issues in Creator View - second round of fixes

**Purpose:** Resolve all outstanding UI/UX issues from Phase-01-Issue-Fix.md (二次修复 version)

**Output:** Fully functional Creator view with correct navigation, layout, and page display
</objective>

<execution_context>
@C:/Users/MA Huan/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@Phase-01-Issue-Fix.md (二次修复版本)

**Critical Issues to Fix:**

1. **Theme Preview Split**: Change from 60/40 to 70/30 (preview/config)

2. **Navigation Single Highlight**: All nav buttons (expandable + simple) should have only ONE highlighted at a time

3. **Navigation Single Expand**: Only one submenu should expand at a time

4. **Scheme Item Highlight**: Selected scheme in submenu should be highlighted (not just active scheme with checkmark)

5. **New Scheme Button Text**: Currently showing code, should show "+ 新建方案" (or "+ New Scheme" in English)

6. **Scrollbar Style**: Thumb color should be brighter

7. **Scrollbar Layout**: Reserve space for scrollbar, don't compress content

8. **Arrow Position**: Fix vertical alignment of expand/collapse arrow

9. **Feature Pages Display**: 
   - Desktop Background, Mouse Click, Shutdown, Boot Restart, Screen Wake should display their views
   - Desktop Clock, Pomodoro, Anniversary should display their views
   - Check why these are not showing

**Existing Views (Already Created):**
- Views/DesktopBackgroundView.xaml ✅
- Views/MouseClickView.xaml ✅
- Views/ShutdownView.xaml ✅
- Views/BootRestartView.xaml ✅
- Views/ScreenWakeView.xaml ✅
- Views/DesktopClockView.xaml ✅
- Views/PomodoroView.xaml ✅
- Views/AnniversaryView.xaml ✅
</context>

<interfaces>
Current ViewModel structure:
- ObservableCollection<SchemeModel> DesktopBackgroundSchemes, MouseClickSchemes, etc.
- bool IsDesktopBackgroundExpanded, IsMouseClickExpanded, etc.
- bool IsThemePreviewSelected, IsDesktopBackgroundSelected, etc.
- SchemeModel SelectedScheme (if exists, or need to add)
- ICommand SelectFeatureCommand, SelectSchemeCommand, CreateNewSchemeCommand
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix New Scheme Button Text</name>
  <files>Views/CreatorView.xaml, Resources/LocalizationResources.xaml</files>
  <action>
Fix the new scheme button to display correct text instead of code.

**Problem:** Button currently shows: "+ {DynamicResource Creator_NewScheme}"

**Fix:** 
1. Check if localization key "Creator_NewScheme" exists in Resources/LocalizationResources.xaml
2. If not, add it:
   ```xml
   <system:String x:Key="Creator_NewScheme">新建方案</system:String>
   ```
3. Change button content from:
   ```xml
   Content="+ {DynamicResource Creator_NewScheme}"
   ```
   to:
   ```xml
   Content="+ 新建方案"
   ```
   OR fix the binding to work correctly

4. Test with 5 expandable features (Desktop Background, Mouse Click, Shutdown, Boot Restart, Screen Wake)
  </action>
  <verify>
    <automated>grep -n "新建方案\|New Scheme" Resources/LocalizationResources.xaml || echo "Check needed"</automated>
  </verify>
  <done>New scheme buttons display "+ 新建方案" correctly</done>
</task>

<task type="auto">
  <name>Task 2: Fix Navigation Single Highlight Logic</name>
  <files>ViewModels/CreatorViewModel.cs</files>
  <action>
Implement unified single-highlight logic for ALL navigation buttons.

**Current Problem:** Expandable buttons and simple buttons have separate highlight logic

**Solution:**
1. Add a property to track currently selected feature:
   ```csharp
   [ObservableProperty]
   private string _selectedFeature = "ThemePreview";
   ```

2. Modify IsThemePreviewSelected, IsDesktopBackgroundSelected, etc. to be computed:
   ```csharp
   public bool IsThemePreviewSelected => SelectedFeature == "ThemePreview";
   public bool IsDesktopBackgroundSelected => SelectedFeature == "DesktopBackground";
   // ... etc for all 9 features
   ```

3. In SelectFeature command, set SelectedFeature = featureName

4. For expandable features, also set IsDesktopBackgroundExpanded = true when selected

5. Ensure ONLY ONE feature is selected at a time
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Only one nav button highlighted at any time</done>
</task>

<task type="auto">
  <name>Task 3: Fix Single Expand Logic</name>
  <files>ViewModels/CreatorViewModel.cs</files>
  <action>
Implement single-expand logic for submenus.

**Current Problem:** Multiple submenus can expand simultaneously

**Solution:**
1. Modify the expanded property setters or add a method:
   ```csharp
   partial void OnIsDesktopBackgroundExpandedChanged(bool value)
   {
       if (value)
       {
           IsMouseClickExpanded = false;
           IsShutdownExpanded = false;
           IsBootRestartExpanded = false;
           IsScreenWakeExpanded = false;
       }
   }
   ```

2. Repeat for all 5 expandable features

3. When clicking simple buttons (Theme Preview, Open App, etc.), collapse ALL submenus:
   ```csharp
   private void CollapseAllSubmenus()
   {
       IsDesktopBackgroundExpanded = false;
       IsMouseClickExpanded = false;
       IsShutdownExpanded = false;
       IsBootRestartExpanded = false;
       IsScreenWakeExpanded = false;
   }
   ```
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Only one submenu expands at a time</done>
</task>

<task type="auto">
  <name>Task 4: Fix Scheme Item Selection Highlight</name>
  <files>ViewModels/CreatorViewModel.cs, Views/CreatorView.xaml</files>
  <action>
Add selected scheme highlight (different from active scheme checkmark).

**Problem:** Currently only shows checkmark for IsActive, not highlight for selected

**Solution:**
1. Add SelectedScheme property to ViewModel:
   ```csharp
   [ObservableProperty]
   private SchemeModel? _selectedDesktopBackgroundScheme;
   // ... for each feature
   ```

2. In SelectSchemeCommand, set the appropriate selected scheme

3. In SchemeItemTemplate, add trigger for selected state:
   ```xml
   <DataTrigger Binding="{Binding DataContext.SelectedDesktopBackgroundScheme.Id, RelativeSource={RelativeSource AncestorType=UserControl}}" 
               Value="{Binding Id}">
       <Setter Property="Background" Value="{StaticResource Accent1Brush}"/>
   </DataTrigger>
   ```

4. Alternative: Add IsSelected property to SchemeModel and bind to that
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Selected scheme item is highlighted in submenu</done>
</task>

<task type="auto">
  <name>Task 5: Fix Theme Preview Split Ratio (70/30)</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Change split ratio from 60/40 to 70/30.

**Current:** 
```xml
<ColumnDefinition Width="6*"/>
<ColumnDefinition Width="4*"/>
```

**Change to:**
```xml
<ColumnDefinition Width="7*"/>
<ColumnDefinition Width="3*"/>
```

Find in CreatorView.xaml (around line 704-705) and update.
  </action>
  <verify>
    <automated>grep -n "7\*\|3\*" Views/CreatorView.xaml | head -5</automated>
  </verify>
  <done>Split ratio is 70/30 (7*/3*)</done>
</task>

<task type="auto">
  <name>Task 6: Fix Scrollbar Style</name>
  <files>Resources/Theme.xaml, Views/CreatorView.xaml</files>
  <action>
Fix scrollbar thumb color and reserve space.

**1. Brighter Thumb Color:**
```xml
<Style TargetType="Thumb">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="Thumb">
                <Border Background="{StaticResource TextSecondaryBrush}"  <!-- Brighter -->
                        CornerRadius="4"
                        Width="4"
                        HorizontalAlignment="Center"/>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>
```

**2. Reserve Space for Scrollbar:**
In CreatorView.xaml ScrollViewer:
```xml
<ScrollViewer Grid.Row="1" 
               VerticalScrollBarVisibility="Auto"
               Padding="0,0,12,0">  <!-- Reserve right padding -->
```

Or use custom style that doesn't overlay content.
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Scrollbar thumb is brighter, space reserved</done>
</task>

<task type="auto">
  <name>Task 7: Fix Arrow Vertical Alignment</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Fix expand/collapse arrow vertical position.

**Current:** Arrow may be positioned too low

**Fix:** Ensure Path has proper vertical alignment in ToggleButton:
```xml
<Path Grid.Column="2"
       Data="M6 9L12 15L18 9"
       Stroke="{StaticResource TextSecondaryBrush}"
       StrokeThickness="2"
       Width="16"
       Height="16"
       Stretch="Uniform"
       VerticalAlignment="Center"  <!-- Ensure this is set -->
       Margin="0"/>  <!-- No extra margin -->
```

Also check ToggleButton style has proper height and content alignment:
```xml
<Setter Property="Height" Value="40"/>
<Setter Property="VerticalContentAlignment" Value="Center"/>
```
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>Arrow is vertically centered in ToggleButton</done>
</task>

<task type="auto">
  <name>Task 8: Fix Feature Pages Display</name>
  <files>ViewModels/CreatorViewModel.cs, App.xaml, Views/*.xaml</files>
  <action>
Debug and fix why feature pages are not displaying.

**Verification Steps:**
1. Check if all View files exist:
   - DesktopBackgroundView.xaml
   - MouseClickView.xaml
   - ShutdownView.xaml
   - BootRestartView.xaml
   - ScreenWakeView.xaml
   - DesktopClockView.xaml
   - PomodoroView.xaml
   - AnniversaryView.xaml

2. Check if DataTemplates are registered in App.xaml

3. Check if DI registrations exist in App.xaml.cs

4. Check LoadFeatureContent method logic:
   - Does it properly create ViewModels?
   - Does it assign ConfigurationContent?
   - Are there any null reference exceptions?

5. Add debug logging to trace the issue:
   ```csharp
   Debug.WriteLine($"Loading feature: {featureName}");
   Debug.WriteLine($"ConfigurationContent set to: {ConfigurationContent?.GetType().Name ?? "null"}");
   ```

6. Fix any issues found (likely DI or DataTemplate issues)
  </action>
  <verify>
    <automated>dotnet build --no-restore 2>&1 | findstr /i "error" || echo "Build successful"</automated>
  </verify>
  <done>All feature pages display correctly when selected</done>
</task>

</tasks>

<verification>
- [ ] New scheme button shows "+ 新建方案"
- [ ] Only one nav button highlighted at a time (expandable + simple unified)
- [ ] Only one submenu expanded at a time
- [ ] Clicking simple button collapses all submenus
- [ ] Selected scheme item is highlighted (different from active checkmark)
- [ ] Theme Preview split is 70/30
- [ ] Scrollbar thumb is brighter color
- [ ] Scrollbar doesn't compress content (space reserved)
- [ ] Expand arrow vertically centered
- [ ] Desktop Background page displays
- [ ] Mouse Click page displays
- [ ] Shutdown/Boot Restart/Screen Wake pages display
- [ ] Desktop Clock/Pomodoro/Anniversary pages display
</verification>

<success_criteria>
All issues from Phase-01-Issue-Fix.md (二次修复) resolved:
- ✅ Theme preview 70/30 split
- ✅ Unified single highlight for all nav buttons
- ✅ Single expand for submenus
- ✅ Scheme item selection highlight
- ✅ Correct new scheme button text
- ✅ Brighter scrollbar thumb
- ✅ Proper scrollbar layout
- ✅ Arrow vertical alignment
- ✅ All feature pages display
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-v2/03-01-SUMMARY.md` documenting all fixes.
</output>
