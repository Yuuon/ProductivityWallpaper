---
phase: 02-data-structure
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - Views/CreatorView.xaml
  - Views/SaveChangesDialog.xaml
  - Views/SaveChangesDialog.xaml.cs
autonomous: false
requirements:
  - DS-005
  - DS-006
  - DS-007
must_haves:
  truths:
    - "Save button appears at top of left navigator bar (right side)"
    - "Save button is enabled only when IsDirty = true"
    - "Save button uses AccentBrush when enabled for visual feedback"
    - "Leave-page confirmation dialog appears when navigating away with unsaved changes"
    - "Confirmation dialog has [Save] [Don't Save] [Cancel] buttons with theme styling"
  artifacts:
    - path: "Views/CreatorView.xaml"
      provides: "Save button in left sidebar with IsDirty binding"
    - path: "Views/SaveChangesDialog.xaml"
      provides: "Themed confirmation dialog for leave-page scenario"
      exports: ["SaveChangesDialog"]
    - path: "Views/SaveChangesDialog.xaml.cs"
      provides: "Code-behind for dialog result handling"
  key_links:
    - from: "Save button"
      to: "CreatorViewModel.SaveThemeCommand"
      via: "Command binding"
    - from: "Save button"
      to: "CreatorViewModel.IsDirty"
      via: "IsEnabled binding"
    - from: "Back button click"
      to: "SaveChangesDialog"
      via: "Show if IsDirty, handle result"
    - from: "Auto-save timer"
      to: "ThemeService.SaveThemeAsync(backup: true)"
      via: "5-minute interval if IsDirty"
---

<objective>
Implement the Save System UI including: Save button with dirty state indicator, leave-page confirmation dialog, and auto-save backup timer. Focus on visual feedback and user experience.

Purpose: Provide clear visual indication of unsaved changes and safe navigation with confirmation options.
Output: Updated CreatorView with Save button, new SaveChangesDialog, and timer infrastructure.
</objective>

<execution_context>
@C:/Users/MA Huan/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/MA Huan/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-structure/02-CONTEXT.md
@Views/CreatorView.xaml
</context>

<interfaces>
<!-- Current CreatorView.xaml structure (key parts) -->

From Views/CreatorView.xaml:
```xml
<!-- Left Sidebar -->
<Grid Grid.Column="0" Background="{StaticResource BackgroundBlockBrush}">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/><!-- Back Button area -->
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    
    <!-- Back Button (currently Row 0) -->
    <Button Grid.Row="0" Command="{Binding BackToWelcomeCommand}" ...>
        ...
    </Button>
</Grid>
```

<!-- Expected CreatorViewModel additions (from 02-05) -->
```csharp
public partial class CreatorViewModel : ObservableObject
{
    [ObservableProperty]
    private bool _isDirty;  // For save button state
    
    [ObservableProperty]
    private bool _isSaving;  // For save button busy state
    
    public IAsyncRelayCommand SaveThemeCommand { get; }
    public IAsyncRelayCommand ExportThemeCommand { get; }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SaveChangesDialog (themed confirmation dialog)</name>
  <files>Views/SaveChangesDialog.xaml, Views/SaveChangesDialog.xaml.cs</files>
  <action>
Create a themed confirmation dialog for leave-page confirmation.

SaveChangesDialog.xaml:
- Window style: Dialog, no resize, centered
- Size: 400 x 200
- Background: BackgroundBlockBrush
- Border: BorderLineBrush with 1px thickness, CornerRadius=10
- Padding: 24

Content structure:
```xml
<StackPanel>
    <TextBlock Text="Unsaved Changes" FontSize=16 FontWeight=SemiBold 
               Foreground=TextPrimaryBrush Margin="0,0,0,12"/>
    <TextBlock Text="You have unsaved changes. Save before leaving?"
               Foreground=TextSecondaryBrush Margin="0,0,0,24"/>
    
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="SaveButton" Content="Save" ... Margin="0,0,8,0"/>
        <Button x:Name="DontSaveButton" Content="Don't Save" ... Margin="0,0,8,0"/>
        <Button x:Name="CancelButton" Content="Cancel" IsDefault="True" .../>
    </StackPanel>
</StackPanel>
```

Button styling:
- Save: PrimaryButtonStyle (AccentBrush background)
- Don't Save: Standard button with BorderLineBrush border
- Cancel: Standard button

SaveChangesDialog.xaml.cs:
```csharp
public partial class SaveChangesDialog : Window
{
    public SaveChangesDialog()
    {
        InitializeComponent();
        Owner = Application.Current.MainWindow;
    }
    
    public SaveChangesResult Result { get; private set; } = SaveChangesResult.Cancel;
    
    private void SaveButton_Click(object sender, RoutedEventArgs e)
    {
        Result = SaveChangesResult.Save;
        DialogResult = true;
    }
    
    private void DontSaveButton_Click(object sender, RoutedEventArgs e)
    {
        Result = SaveChangesResult.DontSave;
        DialogResult = true;
    }
    
    private void CancelButton_Click(object sender, RoutedEventArgs e)
    {
        Result = SaveChangesResult.Cancel;
        DialogResult = false;
    }
}

public enum SaveChangesResult
{
    Save,
    DontSave,
    Cancel
}
```

Add proper namespace and XML documentation.
  </action>
  <verify>
    <automated>MISSING — Build verification only</automated>
  </verify>
  <done>SaveChangesDialog created with three buttons, themed styling, and SaveChangesResult enum</done>
</task>

<task type="auto">
  <name>Task 2: Add Save button to CreatorView left sidebar</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Modify CreatorView.xaml to add Save button in the left sidebar header.

Current structure (Row 0 has Back button):
```xml
<Grid.RowDefinitions>
    <RowDefinition Height="Auto"/><!-- Back Button area -->
    <RowDefinition Height="*"/>
</Grid.RowDefinitions>
```

Update Row 0 to have a Grid with Back button (left) and Save button (right):
```xml
<!-- Header with Back and Save buttons -->
<Grid Grid.Row="0" Margin="16,16,16,8">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/><!-- Back button -->
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/><!-- Save button -->
    </Grid.ColumnDefinitions>
    
    <!-- Back Button (left side) -->
    <Button Grid.Column="0" Command="{Binding BackToWelcomeCommand}" ...>
        <StackPanel Orientation="Horizontal">
            <Path Data="M12 4L4 12L12 20 M6 12H20" Stroke="{StaticResource IconBrush}" .../>
            <TextBlock Text="{DynamicResource Creator_Back}" Foreground="{StaticResource IconBrush}" .../>
        </StackPanel>
    </Button>
    
    <!-- Save Button (right side) -->
    <Button Grid.Column="2" Command="{Binding SaveThemeCommand}"
             IsEnabled="{Binding IsDirty}"
             ToolTip="Save theme changes">
        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="Opacity" Value="0.5"/>
                <Style.Triggers>
                    <!-- Enabled (dirty) state - full opacity with accent -->
                    <Trigger Property="IsEnabled" Value="True">
                        <Setter Property="Opacity" Value="1"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Button.Style>
        <Path Data="M17 3H5a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2V7l-4-4z M7 3v5h8V3 M17 21v-8H7v8"
              Stroke="{Binding IsDirty, Converter={StaticResource BooleanToAccentBrushConverter}}"
              StrokeThickness="2"
              Width="20" Height="20"
              Stretch="Uniform"/>
    </Button>
</Grid>
```

Note: Need to create BooleanToAccentBrushConverter or use Trigger to change Path color when IsDirty=true.

Alternative simpler approach for icon color:
```xml
<Path ...>
    <Path.Style>
        <Style TargetType="Path">
            <Setter Property="Stroke" Value="{StaticResource IconBrush}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirty}" Value="True">
                    <Setter Property="Stroke" Value="{StaticResource AccentBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Path.Style>
</Path>
```

Save icon path data: "M17 3H5a2 2 0 00-2 2v14a2 2 0 002 2h14a2 2 0 002-2V7l-4-4z M7 3v5h8V3 M17 21v-8H7v8"
  </action>
  <verify>
    <automated>MISSING — Build verification only</automated>
  </verify>
  <done>Save button added to CreatorView header (right side of left sidebar) with IsDirty binding</done>
</task>

<task type="auto">
  <name>Task 3: Add Export Theme Package button to Theme Preview</name>
  <files>Views/CreatorView.xaml</files>
  <action>
Add Export Theme Package button to the Theme Preview configuration panel.

Location: In the right-side configuration panel (Column 1 of the split layout), add an Export button.

In the Theme Preview Grid (around line 796-803), add:
```xml
<!-- Right: Theme Preview Configuration -->
<Border Grid.Column="1" ...>
    <StackPanel Margin="16">
        <TextBlock Text="Theme Settings" FontSize=14 FontWeight=SemiBold .../>
        
        <!-- Export button -->
        <Button Command="{Binding ExportThemeCommand}"
                Margin="0,16,0,0"
                Height="36"
                Style="{StaticResource PrimaryButtonStyle}">
            <StackPanel Orientation="Horizontal">
                <Path Data="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4 M7 10l5 5 5-5M12 15V3"
                      Stroke="White" StrokeThickness="2"
                      Width="16" Height="16"
                      Stretch="Uniform"/>
                <TextBlock Text="Export Theme Package"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
        
        <TextBlock Text="Create a complete, portable theme package with all media files."
                   Foreground="{StaticResource TextSecondaryBrush}"
                   FontSize="12"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap"/>
    </StackPanel>
</Border>
```

This provides a clear distinction between:
- Save: Fast, JSON only, keeps absolute paths
- Export: Complete package, copies files, creates relative paths

Update build to verify XAML is valid.
  </action>
  <verify>
    <automated>dotnet build ProductivityWallpaper.csproj --no-incremental 2>&1 | findstr "error" && exit 1 || exit 0</automated>
  </verify>
  <done>Export Theme Package button added to Theme Preview panel with description text</done>
</task>

<task type="checkpoint:human-verify">
  <name>Checkpoint: Verify Save button UI appearance and placement</name>
  <what-built>
Save button in CreatorView left sidebar header (right side) and SaveChangesDialog for leave-page confirmation.
  </what-built>
  <how-to-verify>
1. Launch the application
2. Navigate to Creator page
3. Create a new theme or open existing
4. Verify Save button appears at top of left sidebar, on the right side (opposite from Back button)
5. Verify Save button is initially disabled (dimmed/low opacity)
6. Make a change (import a file, create a scheme)
7. Verify Save button becomes enabled with AccentBrush color
8. Click Back button while IsDirty=true
9. Verify SaveChangesDialog appears with [Save] [Don't Save] [Cancel] buttons
10. Verify dialog has themed colors (BackgroundBlockBrush background, proper styling)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any UI issues found</resume-signal>
</task>

</tasks>

<verification>
1. Build succeeds with 0 errors
2. SaveChangesDialog exists with three buttons and themed styling
3. Save button positioned at top-right of left sidebar
4. Save button has IsDirty binding for enabled state
5. Save button changes color when enabled (AccentBrush)
6. Export button added to Theme Preview configuration panel
</verification>

<success_criteria>
- SaveChangesDialog created with [Save] [Don't Save] [Cancel] buttons and themed styling
- Save button added to CreatorView header (right side of left sidebar)
- Save button binds to CreatorViewModel.IsDirty for enabled state
- Save button uses AccentBrush when enabled for visual feedback
- Export Theme Package button added to Theme Preview panel
- Build succeeds with 0 errors
- Human verification confirms proper UI appearance
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-structure/02-04-SUMMARY.md`
</output>
