<UserControl x:Class="ProductivityWallpaper.Views.AiToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Dynamic Prompt List -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Segments}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="Auto"/> <!-- Delete Button Column -->
                            </Grid.ColumnDefinitions>
                            
                            <!-- 文本框：用户主要编辑区域 -->
                            <TextBox Text="{Binding UserText, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="#333" Foreground="White" Padding="5" BorderThickness="0"
                                     VerticalContentAlignment="Center"/>
                            
                            <!-- 预设选择：选择后只填充文本框，不参与最终拼接 -->
                            <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableKeywords}" 
                                      SelectedItem="{Binding SelectedKeyword}" Margin="5,0,0,0"
                                      Padding="5"/>
                            
                            <!-- 删除按钮 -->
                            <!-- Command 绑定技巧：因为 DataContext 是单个 Item，所以需要向上找 AiToolViewModel -->
                            <Button Grid.Column="2" Content="✕" 
                                    Command="{Binding DataContext.RemoveSegmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Width="30" Margin="5,0,10,0" 
                                    Background="#C42B1C" Foreground="White" BorderThickness="0"
                                    ToolTip="Remove this line"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="+ Add Line" Command="{Binding AddSegmentCommand}" Padding="15,8" Margin="5" Background="#444" Foreground="White" BorderThickness="0"/>
            <Button Content="{DynamicResource Btn_Generate}" Command="{Binding GeneratePromptCommand}" Padding="15,8" Margin="5" Background="#007ACC" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <!-- Preview -->
        <StackPanel Grid.Row="2" Margin="10,0,10,10">
            <TextBlock Text="Final Prompt Preview:" Foreground="#AAA" Margin="0,5,0,2"/>
            <TextBox Text="{Binding FullPrompt}" Height="60" TextWrapping="Wrap" Background="#222" Foreground="#00FF00" FontFamily="Consolas" Margin="0,0,0,10" BorderThickness="0" Padding="5"/>
            <!-- 占位按钮 -->
            <Button Content="Execute AI Generation (Mock)" Command="{Binding ExecuteAiTaskCommand}" Background="#2D2D30" BorderBrush="#555" BorderThickness="1" Foreground="White" Padding="10"/>
        </StackPanel>
    </Grid>
</UserControl>