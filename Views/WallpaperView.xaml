<UserControl x:Class="ProductivityWallpaper.Views.WallpaperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
            <RowDefinition Height="Auto"/> 
        </Grid.RowDefinitions>
        
        <!-- Top Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding CurrentLibPath}" Background="#333" Foreground="#DDD" VerticalContentAlignment="Center" Padding="5" IsReadOnly="True" BorderThickness="0"/>
            <Button Grid.Column="1" Content="..." Command="{Binding BrowseFolderCommand}" Width="40" Margin="5,0,0,0" Background="#444" Foreground="White" BorderThickness="0" ToolTip="Browse Folder"/>
            <Button Grid.Column="2" Content="↻ Scan" Command="{Binding RefreshMetadataCommand}" Margin="5,0,0,0" Padding="15,5" Background="#444" Foreground="White" BorderThickness="0" ToolTip="Scan folder"/>
        </Grid>

        <!-- Media Grid (Scroll disabled horizontally for Grid layout) -->
        <ListBox Grid.Row="1" ItemsSource="{Binding MediaLibrary}" SelectedItem="{Binding SelectedMedia}"
                 Background="Transparent" BorderThickness="0" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Width="160" Height="100" Background="#222" CornerRadius="4" BorderBrush="#444" BorderThickness="1">
                            <Grid>
                                <Image Source="{Binding DisplayThumbnail}" Stretch="UniformToFill"/>
                                <Border Background="#AA000000" VerticalAlignment="Top" HorizontalAlignment="Right" CornerRadius="0,4,0,4" Padding="4,2">
                                    <TextBlock Text="{Binding Type}" Foreground="White" FontSize="10"/>
                                </Border>
                            </Grid>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding DisplayName}" Width="160" Foreground="#EEE" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" Margin="0,5,0,0" ToolTip="{Binding DisplayName}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Bottom Bar -->
        <Border Grid.Row="2" Background="#252526" Padding="10" CornerRadius="4" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Monitor:" Foreground="#AAA" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox ItemsSource="{Binding Monitors}" SelectedItem="{Binding SelectedMonitor}" DisplayMemberPath="DisplayText" Width="150" Padding="5"/>
                </StackPanel>

                <!-- Checkbox 绑定了 IsEnabled -->
                <CheckBox Grid.Column="1" Content="Auto Theme Color" 
                          IsChecked="{Binding IsAutoThemeColor}" 
                          IsEnabled="{Binding CanUseAutoTheme}"
                          Foreground="{Binding IsEnabled, RelativeSource={RelativeSource Self}, Converter={StaticResource BooleanToBrushConverter}}" 
                          VerticalAlignment="Center" Margin="20,0,0,0">
                    <CheckBox.Style>
                        <Style TargetType="CheckBox">
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>

                <Button Grid.Column="3" Content="Apply Wallpaper" Command="{Binding ApplyWallpaperCommand}" Padding="30,8" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>