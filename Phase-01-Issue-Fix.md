# Phase 01 问题修复需求文档 -- 二次修复

所有问题集中在CreatorView页面下

* 主题预览
主题预览页面左右分割比例仍然不符合要求，请按7：3进行分割；

* 左侧导航
    * 这一条仍未修复：二级菜单的勾号不是指当前选中的页面，而是指当前激活的方案，这个激活按钮和具体页面里面的标题右侧的激活按钮相关联；需要说明的是：这里的激活不是指选中的页面，而是壁纸系统内的功能激活相关，如果你需要在具体的功能开发时再扩展相关内容，可以先不处理这个打勾的问题，但是二级菜单选中的按钮需要做高亮化处理，以明确当前正在哪个项目的页面；
    * 新建方案按钮现在显示一串代码一样的东西，具体是什么？这里应该只要类似“+新建方案”这样的文字按钮就好了吧？
    * 左侧导航区域扩展显示滚动条时，滑块颜色稍微再亮一点，现在有点暗；显示滚动条时，避免压缩左侧区域的位置，事先留出空间，但是只有需要时再显示；
    * 导航按钮单一展开未修复，现在还是各自展开；并且可展开按钮和非可展开按钮的单一高亮逻辑仍未合并；再次强调，所有左侧导航栏按钮，一次应该只有一个处于高亮状态；
    * 该问题未修复：二级菜单展开后，导航按钮右侧箭头帽位置偏低；

* 页面显示错误
二级菜单页面仍未修复，比如点击导航“鼠标点击”按钮，展开后新建方案，再点击“鼠标点击 1”（默认名称），应该显示从“ReferenceResources/创作主题 - 鼠标点击 - 空.svg”等文件生成而来的UI页面，但是现在没有。先确认这些页面确实按照文档SVG2XAML_GUIDE_2中生成了对应的View，并以列表的形式列出来，然后再检查问题出在哪里。另外时钟番茄钟纪念日页面显示功能在上次的修复中被破坏，现在点击无法正常显示页面了。