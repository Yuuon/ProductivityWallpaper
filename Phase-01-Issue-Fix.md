# Phase 01 问题修复需求文档 -- 六次修复

所有问题集中在CreatorView页面下

* 左侧导航
    * 未完成：二级菜单勾号是去掉了，但是选中条目需要的高亮色没有设置；
    * 未完成：显示滚动条时，避免压缩左侧区域的位置，事先留出空间，但是只有需要时再显示，显示时不要压缩导航按钮宽度；
    * 未完成：左侧导航按钮高亮逻辑仍然有问题，请按以下逻辑重新处理：所有导航按钮（包括可展开，不可展开）都在一个逻辑组中，该逻辑组同一时间只能有一个项目处于启用状态，处于启用状态时显示高亮，非启用状态时不显示高亮
    * 未完成：二级菜单展开后，导航按钮右侧箭头帽位置偏低，低于按钮文字最底部；

* 页面显示错误
右侧页面显示仍然是错误的！！！目前除了主题预览其他所有页面都无法正常显示！！！仍然未能修复！！！